# Orclaw — Python dependencies (planned, install in sprint 1)

# Agent framework
claude-agent-sdk>=0.0.10        # Official Anthropic SDK for agentic workflows
anthropic>=0.40.0               # Direct API client (for token accounting)

# CLI
click>=8.1.0                    # CLI framework
rich>=13.7.0                    # Pretty terminal output
typer>=0.12.0                   # Typer wraps Click, nicer for subcommands

# GitHub
PyGithub>=2.3.0                 # API client (we still shell out to gh for some things)

# Async / orchestration
anyio>=4.3.0                    # Async runtime
httpx>=0.27.0                   # HTTP client

# State
# (sqlite3 is stdlib — no dependency needed)

# Web dashboard (optional, sprint 5)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0

# Observability
structlog>=24.1.0               # Structured logging

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
ruff>=0.4.0                     # Lint + format
mypy>=1.10.0

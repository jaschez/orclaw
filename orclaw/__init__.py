"""orclaw — multi-agent engineering pipeline for ${TARGET_REPO}.

Public API surface is intentionally minimal at this stage. The CLI is the
primary interface; individual modules are not part of the public contract yet.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
